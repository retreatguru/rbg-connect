actor: Tester
paths:
    tests: /  # requires something otherwise get error generating cest
    log: _output
    data: _data
    helpers: _support
settings:
    bootstrap: _bootstrap.php
    colors: true
    memory_limit: 1024M
modules:
    config:
        Db:
            dsn: 'mysql:host=localhost;dbname=programsconnect'
            user: 'root'
            password:
            dump: _data/dump.sql
            populate: true
            cleanup: true
